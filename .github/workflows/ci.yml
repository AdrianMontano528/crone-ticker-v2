# name: CI

# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]
#   workflow_dispatch:

# jobs:
#     run-tests:
#         name: Run tests
#         runs-on: ubuntu-latest
#         steps:
#             - name: Checkout code
#               uses: actions/checkout@v4

#             - name: Set up Node.js 20
#               uses: actions/setup-node@v4
#               with:
#                 node-version: '22.21.1'

#             - name: Install Dependencies
#               run: npm ci

#             - name: Run Tests
#               run: npm test



name: CI Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  NODE_VERSION: "22.21.1"
  APP_NAME: cron-ticker-v2

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     env:
#       BUILD_DIR: dist

#     steps:
#       - uses: actions/checkout@v4

#       - name: Show variables
#         run: |
#           echo "NODE_VERSION: ${{ env.NODE_VERSION }}"
#           echo "APP_NAME: ${{ env.APP_NAME }}"
#           echo "BUILD_DIR: ${{ env.BUILD_DIR }}"

#       - name: Show github secrets
#         run: |
#           echo "API_KEY: ${{ secrets.API_KEY }}" 

#       - name: Show github functions
#         run: |
#           echo ${{ toJSON(github) }}" 

#       - name: Build
#         run: npm run build
#         env:
#           APP_VERSION: "3.0.0"

jobs:
  lint:
    name: Lint Code
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
      - run: npm ci
      - run: npm run build

  test:
    name: Run Tests
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
      - run: npm ci

      - name: Run Tests with Coverage
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-${{ github.sha }}
          path: coverage/
          retention-days: 3
      - run: npm run test:coverage